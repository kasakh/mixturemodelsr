% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmm.R
\name{mm_tmm_fit}
\alias{mm_tmm_fit}
\title{Fit a t-Mixture Model (TMM)}
\usage{
mm_tmm_fit(x, k, optimizer = "Newton-CG", scale = 0.5, ...)
}
\arguments{
\item{x}{Numeric matrix or data.frame (rows = observations, columns = features)}

\item{k}{Number of mixture components}

\item{optimizer}{Optimizer name: "Newton-CG" (default), "grad_descent",
"rms_prop", or "adam"}

\item{scale}{Initialization scale parameter (default 0.5)}

\item{...}{Additional arguments passed to Python init_params() or fit() methods}
}
\value{
An mm_fit object
}
\description{
Fits a mixture of multivariate t-distributions to data using gradient-based optimization.
T-distributions are more robust to outliers than Gaussian distributions.
}
\details{
T-mixture models use multivariate t-distributions instead of Gaussians,
making them more robust to outliers. Each component has its own degrees of
freedom parameter, allowing different tail behaviors.
}
\examples{
\dontrun{
# Fit TMM with 3 components (robust to outliers)
fit <- mm_tmm_fit(iris[, 1:4], k = 3)
mm_bic(fit)

# Get cluster labels
labels <- mm_predict(fit)
table(labels, iris$Species)
}
}
